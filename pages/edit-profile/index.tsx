import React from 'react'
import UI from 'UI/profile'
import Container from '@mui/material/Container'

const IndexPage = (): JSX.Element => {
  return <Container sx={{ width: { xs:1, sm: 0.6 } }}><UI /></Container>
}

export default IndexPage
